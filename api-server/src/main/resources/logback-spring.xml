<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <include resource="org/springframework/boot/logging/logback/base.xml"/>

    <!-- Use Spring Boot's default CONSOLE appender with pattern (overridable via logging.pattern.console) -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>